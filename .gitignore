# Dependencies
node_modules/
package-lock.json

# Generated deploy output / editor / OS noise
_site/
dist/
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Transient translation working files — regenerated by tools/, not committed.
# locales/pending-translations.json is deliberately NOT ignored: it is the
# persistent record of what still needs translating and belongs in git.
tmp/

# Local env
.env
.env.local
